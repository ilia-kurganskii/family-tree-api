version: '3.7'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.e2e
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      - mongo
    env_file:
      - .e2e.env

  mongo:
    image: mongo:latest
    env_file:
      - .e2e.env

